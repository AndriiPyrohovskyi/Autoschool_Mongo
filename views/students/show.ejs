<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <title>Студент - <%= student.firstName %> <%= student.lastName %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <%- include('../partials/navbar') %>
    
    <div class="container mt-4">
        <div class="row">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h4><i class="fas fa-user"></i> <%= student.firstName %> <%= student.lastName %></h4>
                        <div>
                            <a href="/students/<%= student._id %>/edit" class="btn btn-warning btn-sm">
                                <i class="fas fa-edit"></i> Редагувати
                            </a>
                            <a href="/students" class="btn btn-secondary btn-sm">
                                <i class="fas fa-list"></i> До списку
                            </a>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <p><strong>Email:</strong> <%= student.email %></p>
                                <p><strong>Телефон:</strong> <%= student.phone %></p>
                                <p><strong>Дата народження:</strong> <%= new Date(student.dateOfBirth).toLocaleDateString('uk-UA') %></p>
                            </div>
                            <div class="col-md-6">
                                <p><strong>Категорія:</strong> 
                                    <span class="badge bg-primary"><%= student.category %></span>
                                </p>
                                <p><strong>Статус:</strong> 
                                    <span class="badge bg-<%= student.status === 'active' ? 'success' : student.status === 'completed' ? 'info' : 'warning' %>">
                                        <%= student.status === 'active' ? 'Активний' : student.status === 'completed' ? 'Завершено' : 'Призупинено' %>
                                    </span>
                                </p>
                                <p><strong>Дата запису:</strong> <%= new Date(student.enrollmentDate).toLocaleDateString('uk-UA') %></p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>